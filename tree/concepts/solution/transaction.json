{
  "name": "事务",
  "detail": "做法:数据库事务(ACID)包住一组写,要么全成要么全不成;跨服务用 saga 拆成本地事务",
  "doc": "边界:事务只在单库内免费;跨服务跨库时 2PC 代价极高,工程上用 saga——拆成一串本地事务,每步配补偿动作,失败时逆序撤回。\n\n常见错误:事务包太大(里面做 RPC、耗时计算)拖垮连接池;在事务里发消息导致「提交前消息已出」——用 outbox 模式(消息先落库,relay 异步投递)。\n\n最后防线:定时对账,兜住一切漏网的不一致。"
}
