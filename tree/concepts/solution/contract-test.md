### 为什么

内部实现随便换,只要契约不破,调用方就安全。它把「不敢改」变成「放心改」,是重构的兜底网。黄金标准:重构时契约测试不需要改。

### 反例

别 mock 自己的私有方法——那等于把测试绑死在实现上,重构时测试全碎,比没测试更劝退。

### 扩展

消费者驱动契约(pact)由调用方写下期望,提供方发版前必须跑过——跨团队接口的保障。

### 局限

保证「接口不破」,不保证「实现变好」;它是安全网,不是设计指南。
