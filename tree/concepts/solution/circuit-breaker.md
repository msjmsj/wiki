### 状态机

闭合(正常)→ 失败超阈值 → 开路(直接快速失败)→ 冷却 → 半开(放少量请求试探)→ 成功闭合,失败再开路。

### 为什么

故障期持续重试等于往着火的房子浇水——熔断给对方恢复时间,也保护自己的线程池不被拖光。

### 现成库

resilience4j、Polly、Sentinel,别手写。

### 搭配

熔断负责「快速失败」,降级给出保底响应;关键路径开路必须配告警。
