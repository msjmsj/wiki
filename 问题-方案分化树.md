# 问题 → 方案 分化树

> **用途**：开发中遇到痛苦时，从树根往下走，每个节点回答一个问题，走到叶子就是方案。
> **为什么用树不用表**：症状是无限的，平表会无限加行；树的骨架基于**根源**（有限且稳定），新痛苦只是新叶子——**骨架不动，只长叶子**。
> **维护规则**：见文末"如何让这棵树不腐烂"。

---

## 树根：第一个分化问题

```
我的程序让我痛苦
│
└─ Q1：谁在受折磨？
   │
   ├─ 读代码/改代码的人 ──→ 【A 心智病】组织代码可解
   ├─ 运行的机器 ────────→ 【B 物理病】要控制时序与副本
   └─ 协作的组织 ────────→ 【C 社会病】要对齐结构、保留选择权
```

---

## A. 心智病：看不懂 / 改不动（折磨人，机器无所谓）

```
A. 心智病
│
├─ A1. 看不懂
│   │
│   ├─ 单个东西太大（长函数、大文件，看半小时才敢改）
│   │   → 拆分 · 单一职责 · 深模块（小接口藏大实现）
│   │     📎 反面教材：panel.ts 1479 行
│   │
│   └─ 东西之间的关系乱（依赖像蜘蛛网）
│       → 分层 · 依赖方向管理 · 外观模式 · 模块只露 index.ts
│
└─ A2. 改不动
    │
    ├─ 一处变化要改很多地方（霰弹式修改）
    │   → 信息隐藏：把"易变的决策"藏进一个模块（Parnas）
    │
    ├─ 同一个修改要在多处重复
    │   → DRY · 抽象复用（⚠️ 只是"长得像"的代码别硬合并）
    │
    ├─ 加新功能必须改老代码（if-else 越接越长）
    │   → 开放封闭：多态 · 策略模式 · 查表驱动 · 装饰器 · 中间件
    │
    ├─ 两个接口对不上，又改不了其中一方
    │   → 适配器模式    📎 convertSnapshotToTokenHolder()
    │
    ├─ 创建对象写死类名，换实现要全局搜 new
    │   → 工厂 · 依赖注入（构造函数传参）
    │
    ├─ 一变要通知多方，直接调用缠成网
    │   → 观察者 · 发布订阅（⚠️ 代价：数据流不透明、难调试）
    │     📎 hook.ts 的 CustomEvent
    │
    ├─ 全局实例到处被 import，测试难写
    │   → 受控单例 / 显式注入（能传参就别用单例）📎 scoreManager
    │
    └─ 不敢改，怕改坏 ──（也可能根源在 C2，交叉引用）
        → 契约测试：只测公开接口，给重构兜底
```

---

## B. 物理病：结果不对 / 外部掉链子（折磨机器，与人聪不聪明无关）

```
B. 物理病
│
├─ B1. 状态出问题（"现在到底是什么、谁改的"）
│   │
│   ├─ 不知道谁改了数据
│   │   → 不可变 · 单一数据源 · 单向数据流（reducer 纯函数）
│   │
│   ├─ 多线程/异步抢同一份数据（竞态）
│   │   → 不可变 · 消息传递代替共享 · 锁纪律
│   │
│   ├─ 有副本：副本与源头不一致
│   │   → 缓存策略：TTL（📎 别名缓存 30min）· 失效优先于更新
│   │     （⚠️ 缓存 = 用一致性换速度，先问能不能容忍旧数据）
│   │
│   ├─ 历史丢了，无法审计回放
│   │   → 事件溯源 · Memento · 活动日志  📎 activityLogger
│   │
│   └─ 执行一半挂了，数据半新半旧
│       → 事务 · 补偿动作 · 定时对账
│
├─ B2. 通信出问题（和不靠谱的外部世界打交道）
│   │
│   ├─ 外部会失败
│   │   → 超时 · 重试（指数退避）· 熔断 · 降级
│   │
│   ├─ 外部会重复（重复提交 / 重复消费）
│   │   → 幂等设计 · 去重（重复执行，结果相同）
│   │
│   ├─ 外部会变化（接口/字段偷偷变，程序静默出错）
│   │   → 防腐层 + Schema 校验：进系统即翻译，变化只改一处
│   │     📎 见 docs/architecture/模块化架构设计.md
│   │
│   └─ 外部会丢失（异步消息没了没人知道）
│       → 确认 ack · 重投 · 死信队列兜底 · 消费端幂等
│
└─ B3. 规模出问题（一台机器/一个进程装不下）
    │
    ├─ 太频繁，资源吃紧
    │   → 节流 · 防抖 · 批处理  📎 刷新间隔 0.1s–5s 可调
    │
    ├─ 太慢
    │   → 缓存 · 异步 · 并行 · 索引
    │
    └─ 存不下 / 怕单点故障
        → 分片 · 副本 · 分治
```

---

## C. 社会病：协作与演进（多人、长期才出现）

```
C. 社会病
│
├─ C1. 协作冲突
│   │
│   ├─ 多人改同一文件天天冲突
│   │   → Conway：边界对齐团队 · 一个模块一个 owner
│   │
│   └─ 方案评审吵不出结论
│       → ADR：一页纸记"为什么"，评审有据可依
│
├─ C2. 演进僵化
│   │
│   ├─ 老代码没人敢动
│   │   → 契约测试 · 可逆设计 · known-violations 基线（只减不增）
│   │
│   └─ 新功能不知道放哪，全堆进大文件
│       → 架构红线 + 脚手架（让正路比野路快）
│         📎 docs/architecture/架构守护指南.md
│
└─ C3. 知识失传
    │
    ├─ 新人 / AI 上手就破坏结构
    │   → 写进 CLAUDE.md · CI 强制（ESLint boundaries，保存即报错）
    │
    └─ 忘记当初为什么这么做
        → ADR · 决策记录
```

---

## 反向索引：手里有药 → 挂在哪片叶子

（方案是有限的，这部分用平表没问题）

| 方案 | 位置 |
|---|---|
| 观察者 / 发布订阅 | A2 · 一变通知多方 |
| 策略 / 查表驱动 | A2 · 加功能不改老代码 |
| 适配器 | A2 · 接口对不上 |
| 外观 | A1 · 关系乱 |
| 工厂 / 依赖注入 | A2 · 创建写死 |
| 不可变 / 单向数据流 | B1 · 不知道谁改的 |
| 事件溯源 | B1 · 历史丢了 |
| 幂等 | B2 · 外部会重复 |
| 重试 / 熔断 | B2 · 外部会失败 |
| 防腐层 + Schema | B2 · 外部会变化 |
| TTL 缓存 | B1 · 副本不一致 / B3 · 太慢 |
| 契约测试 | A2 · 不敢改 / C2 · 老代码没人动 |
| ADR | C1 · 评审吵架 / C3 · 忘记为什么 |

---

## 如何让这棵树不腐烂（维护规则）

1. **新痛苦来了，先走树**：从根回答三个问题定位到分支，把新症状/方案写成叶子（1-2 行）。
2. **叶子只增不删、不重构**——叶子可以无限多，每片都很小，树不会失控。
3. **骨架（节点的分化问题）极少变**。如果新痛苦找不到分支，先怀疑自己走错了路；确认骨架真的缺分支，是一件**架构级事件**——写 ADR 记录再改。
4. **一个症状挂在多处是正常的**：症状 ≠ 根源（如"不敢改"同时在 A2 和 C2）。交叉引用即可，不要为消除重复而合并分支。
5. **反向索引只收录"有名字的方案"**，无名的一次性技巧进叶子，不进索引。

## 相关文档

- [模块化架构设计](./architecture/模块化架构设计.md)——B2"外部会变化"的完整方案
- [架构守护指南](./architecture/架构守护指南.md)——C2/C3 的落地机制
